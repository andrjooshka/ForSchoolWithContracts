<t:container xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd" xmlns:p="tapestry:parameter">
	<t:if test="updatemode">
		Тип занятия ${etype.title} : Редактирование
		<p:else>
			Новый тип занятия
		</p:else>
	</t:if> 
	<div>
	
	<t:beaneditform object="etype" submitlabel="message:submit-label" exclude="id"/>
	</div>
</t:container>